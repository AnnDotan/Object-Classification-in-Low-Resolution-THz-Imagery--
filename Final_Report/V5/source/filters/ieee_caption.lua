-- ieee_caption.lua
-- Forces table captions ABOVE the table (IEEE convention) and figure
-- captions BELOW the figure (already the default for DOCX). Currently
-- this only verifies the convention; Pandoc's default DOCX writer
-- already places captions correctly when fed LaTeX source.
return {}
