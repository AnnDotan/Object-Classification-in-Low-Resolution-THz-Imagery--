# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg

# Virtual Environments
venv/
.venv/
.venv-gpu/
ENV/

# IDE
.vscode/
.history/
.DS_Store
desktop.ini

# Jupyter
.ipynb_checkpoints/

# Data & Datasets
/data/
datasets/
raw_data/
*.zip
*.tar.gz
*.npy
*.npz

# ─── Model checkpoints & weights (US-013) ───────────────────────────
# Block binary weight artifacts everywhere.
*.pth
*.pt
*.ckpt
*.bin
*.safetensors
*.onnx
*.h5
*.weights
*.pkl
*.msgpack
checkpoints/
weights/
torch_hub/
.cache/
runs/final/**/*.ckpt
runs/systematic/**/*.ckpt

# ─── Training outputs ───────────────────────────────────────────────
runs/
logs/
results/
outputs/
*.log
!scripts/notebooklm_sync.log
wandb/
lightning_logs/

# ─── artifacts/ — explicit per-subpath excludes ─────────────────────
# We do NOT blanket-exclude artifacts/ because gitignore semantics
# prevent re-including children of an outright-excluded directory.
# Listing subpaths explicitly leaves artifacts/priors/ (paper-anchored
# Optuna search bounds; US-003) implicitly tracked.
artifacts/weights/
artifacts/optuna_thz.db
artifacts/dashboard_thumbs/
# US-019B v1 thumbnail blob snapshot — manifest under
# artifacts/validation/v1_thumbs_manifest.json is the durable evidence;
# the PNG blobs are local-only (operator visual gate runs on this disk).
artifacts/validation/v1_thumbs/
artifacts/best_hparams/
# artifacts/Final_Exp.html is committed by SYNCHRONIZER at phase boundaries (US-016).
# It IS the canonical Gold Standard dashboard snapshot — must be tracked so the
# remote always reflects the latest committed campaign state.
artifacts/figures/
artifacts/summaries/
artifacts/tables/
artifacts/dashboard_*.html
artifacts/all_phases_results.json
artifacts/systematic_results.json
artifacts/final_plan_results.json
artifacts/experiment_log.txt
artifacts/sample_inputs.png
*.optuna_journal.log

# Papers (local references, large PDFs)
papers/
